# VSCode — personal workspace settings (chứa absolute paths, không share)
/.vscode/settings.json

.kiro/

# Flutter build artifacts (old fe/ folder remnants after restructure)
fe/
.dart_tool/
.flutter-plugins-dependencies
build/
.env
